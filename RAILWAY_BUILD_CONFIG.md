# 🔧 Railway Build 配置指南

## 您看到的标签说明

- **Source**: 源代码配置
- **Networking**: 网络和域名设置
- **Build**: 构建配置（这里可以设置 Build Command）
- **Deploy**: 部署设置
- **Danger**: 危险操作（删除等）

## ✅ 配置 Build Command

### 方法 1: 在 "Build" 标签中配置

1. **点击 "Build" 标签**
2. **查找以下选项之一**：
   - "Build Command"
   - "Build Settings"
   - "Custom Build Command"
   - "Override Build Command"
3. **输入以下命令**：
   ```
   cd backend && npm install && npm run init-db
   ```
4. **保存**（点击 "Save" 或 "Update"）

### 方法 2: 在 "Deploy" 标签中配置

1. **点击 "Deploy" 标签**
2. **查找**：
   - "Build Command"
   - "Build Settings"
   - "Pre-deploy Script"
3. **输入**：
   ```
   cd backend && npm install && npm run init-db
   ```
4. **保存**

## 📝 完整的 Build Command

```
cd backend && npm install && npm run init-db
```

这个命令会：
1. 进入 `backend` 目录
2. 安装所有依赖（npm install）
3. 运行数据库初始化脚本（npm run init-db）

## ⚠️ 注意事项

- 保存后，Railway 会自动触发新的部署
- 初始化脚本只需要运行一次
- 如果已经初始化过，再次运行是安全的（会检查表是否已存在）

## 🔄 如果 Build Command 不工作

如果设置 Build Command 后仍然无法初始化数据库，可以：

1. **在 "Deploy" 标签查看部署日志**
   - 查看是否有错误信息

2. **或者使用数据库管理界面**（更直接）
   - 点击 MySQL 服务
   - 使用 SQL 编辑器执行 `init-database.sql` 中的 SQL

---

**配置完成后，Railway 会自动重新部署并初始化数据库！** 🚀
