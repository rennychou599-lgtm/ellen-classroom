# 🚨 Railway 限制计划解决方案

## ⚠️ 问题说明

您的 Railway 账号是**免费/限制计划**，只能部署数据库，**不能部署应用服务**。

这就是为什么：
- ✅ MySQL 数据库可以部署（Active）
- ❌ 后端服务无法部署（显示 "limited access"）

## 🔧 解决方案

### 方案 1: 升级 Railway 计划（推荐，如果预算允许）

1. **访问 Railway 设置**
   - 点击右上角头像 → "Settings"
   - 或访问：https://railway.app/account

2. **升级计划**
   - 选择 "Developer" 或 "Pro" 计划
   - 最低约 $5/月起
   - 可以部署应用和数据库

### 方案 2: 使用其他免费托管服务（推荐，免费）

#### 选项 A: Render（推荐）

**优点**：
- 免费计划可用
- 支持 Node.js 和 MySQL
- 自动部署
- 提供免费 SSL

**步骤**：
1. 访问：https://render.com
2. 使用 GitHub 登录
3. 创建 "New Web Service"
4. 连接您的 GitHub 仓库
5. 配置：
   - Build Command: `cd backend && npm install`
   - Start Command: `cd backend && npm start`
6. 添加 MySQL 数据库
7. 配置环境变量

#### 选项 B: Fly.io

**优点**：
- 免费额度充足
- 全球部署
- 支持 MySQL

**步骤**：
1. 访问：https://fly.io
2. 安装 Fly CLI
3. 部署应用

#### 选项 C: Vercel + Railway MySQL

**优点**：
- Vercel 免费托管前端和 API
- Railway 只用于 MySQL（免费）

**步骤**：
1. 前端部署到 Vercel（免费）
2. 后端 API 也部署到 Vercel（免费）
3. MySQL 继续使用 Railway（免费）

### 方案 3: 使用 Railway 免费额度（如果可用）

检查是否有免费额度：
1. 进入 Railway 项目
2. 查看是否有免费额度可用
3. 如果有，可以临时部署

## 🚀 推荐方案：使用 Render（免费）

### 详细步骤：

#### 1. 注册 Render

1. 访问：https://render.com
2. 点击 "Get Started for Free"
3. 使用 GitHub 账号登录

#### 2. 创建 Web Service

1. 点击 "New +" → "Web Service"
2. 连接您的 GitHub 仓库：`rennychou599-lgtm/ellen-classroom`
3. 配置：
   - **Name**: `ellen-classroom-backend`
   - **Region**: 选择离您最近的区域
   - **Branch**: `main`
   - **Root Directory**: 留空（或填 `backend`）
   - **Build Command**: `cd backend && npm install`
   - **Start Command**: `cd backend && npm start`
   - **Environment**: `Node`

#### 3. 添加 MySQL 数据库

1. 在 Render Dashboard，点击 "New +" → "PostgreSQL" 或 "MySQL"
2. 选择 MySQL
3. 配置数据库名称
4. 复制数据库连接信息

#### 4. 配置环境变量

在 Web Service 的 "Environment" 标签中，添加：

```
PORT=10000
NODE_ENV=production
JWT_SECRET=8196d2dd7261216890f18294788deba3c116afa4960f1621342db1df0b8f7bb5
FRONTEND_URL=https://您的服务名.onrender.com
DB_HOST=您的MySQL主机
DB_USER=您的MySQL用户
DB_PASSWORD=您的MySQL密码
DB_NAME=您的数据库名
DB_PORT=3306
```

#### 5. 初始化数据库

使用 Render 的 Shell 功能或 MySQL 客户端连接数据库，执行 `init-database.sql` 中的 SQL。

## 📝 或者：继续使用 Railway MySQL + 其他服务部署后端

### 方案：Railway MySQL + Render/Vercel 后端

1. **MySQL 继续使用 Railway**（免费，已部署）
2. **后端部署到 Render 或 Vercel**（免费）
3. **前端部署到 Vercel**（免费）

这样可以利用 Railway 的免费 MySQL，同时使用其他免费服务部署应用。

## 🎯 快速决策

- **有预算**：升级 Railway 计划（最简单）
- **无预算**：使用 Render 部署（推荐，完全免费）
- **混合方案**：Railway MySQL + Vercel 后端 + Vercel 前端

---

**建议使用 Render，它是完全免费的，功能强大！** 🚀

需要我帮您配置 Render 部署吗？
